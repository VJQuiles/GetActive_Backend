// The purpose of these enums is to make it easier to add to the various types as i scale this applicaiton up. 

const workoutTypes = ['Full Body', 'Upper', 'Lower', 'Push', 'Pull', 'Leg']
const liftTypes = ['Compound', 'Accessory']
const equipmentTypes = ['Barbell', 'Dumbbell', 'Machine']


module.exports = {
    workoutTypes,
    liftTypes,
    equipmentTypes
}